# Auto-generated during install — DO NOT EDIT MANUALLY after setup
# Run: openssl rand -hex 32 to generate secrets

POSTGRES_USER=mailforge
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=mailforge
DATABASE_URL=postgresql://mailforge:CHANGE_ME_STRONG_PASSWORD@postgres:5432/mailforge

REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
REDIS_URL=redis://:CHANGE_ME_REDIS_PASSWORD@redis:6379

# Auth secrets (generated during install)
AUTH_SECRET=
AUTH_URL=

NEXTAUTH_SECRET=CHANGE_ME_32_CHAR_SECRET
NEXTAUTH_URL=https://your-domain.com

ENCRYPTION_KEY=CHANGE_ME_32_CHAR_HEX

# Install guard
INSTALL_COMPLETE=false
INSTALL_SECRET=CHANGE_ME_INSTALL_GUARD_TOKEN

# App URL (must be set before install)
NEXT_PUBLIC_APP_URL=https://your-domain.com